services:
  relay:
    image: scsibug/nostr-rs-relay:latest
    container_name: nostr-tun-smoke-relay
    ports:
      - "7777:8080"
    volumes:
      - relay-data:/usr/src/app/db
    restart: unless-stopped

volumes:
  relay-data:
